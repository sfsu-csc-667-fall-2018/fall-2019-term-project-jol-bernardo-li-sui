extends layout

block content
    .body
        img(src='images/Logo.png', alt='uno-logo' class='header_logo')
        .login-signup
                a(href='/users/login') Login
                a(href='/users/signup') Signup
        .chat
        .start-page-buttons
            a(href='/session' class='start-button') Start
            a(href='/join' class='join-button') Join
    script(src='/js/deck.js')